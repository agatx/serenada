import Foundation

/// Serenada SDK core — headless call engine.
/// This module will contain signaling, WebRTC, media control,
/// and call state management.
public enum SerenadaCoreMarker {
    public static let version = "0.1.0"
}
