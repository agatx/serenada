import Foundation
import SerenadaCore

/// Serenada SDK call UI — pre-built call flow screens.
/// This module will contain SwiftUI views for the call experience.
public enum SerenadaCallUIMarker {
    public static let version = SerenadaCoreMarker.version
}
