import Foundation
import SerenadaCore

/// Serenada SDK call UI — pre-built call flow screens.
public enum SerenadaCallUI {
    @MainActor public static var version: String { SerenadaCore.version }
}
