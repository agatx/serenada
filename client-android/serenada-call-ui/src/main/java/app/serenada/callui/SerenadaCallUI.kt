package app.serenada.callui

import app.serenada.core.SerenadaCore

/**
 * Serenada SDK call UI — pre-built Compose call flow screens.
 * This module will contain Compose UI for the call experience.
 */
object SerenadaCallUIMarker {
    val VERSION = SerenadaCore.VERSION
}
