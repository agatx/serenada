# Coturn Configuration - Production

# Listener port for UDP and TCP
listening-port=3478
tls-listening-port=5349

# Realm (usually your domain name)
realm=connected.dowhile.fun

# Shared Secret for Time-Limited Credentials
use-auth-secret
static-auth-secret=very-secret-production-key-1234

# External IP (Important! Replaced by env or start script usually, but for fixed VPS config:
external-ip=46.224.118.74

# Min/Max ports for media relay
min-port=49152
max-port=49200

# Logging
verbose
fingerprint
lt-cred-mech
