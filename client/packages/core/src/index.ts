/**
 * @serenada/core — headless call engine.
 * This package will contain signaling, WebRTC, media control,
 * and call state management as vanilla TypeScript (no React).
 */
export const SERENADA_CORE_VERSION = '0.1.0'
